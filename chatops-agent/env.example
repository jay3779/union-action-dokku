# ChatOps Agent Environment Configuration Template
# Copy this file to .env and update values for your deployment

# =============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# =============================================================================

# Application Environment
ENVIRONMENT=production
# Options: production, development, staging

# Logging Configuration
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR

# Union Action API Configuration
UNION_ACTION_API_URL=http://localhost:8000
# Internal URL for bundled Union Action API service

# Port Configuration (set by Dokku)
PORT=8080
# Port number for the application (automatically set by Dokku)

# =============================================================================
# OPTIONAL ENVIRONMENT VARIABLES
# =============================================================================

# Logging Configuration
LOG_SAMPLE_RATE=0.1
# Sample rate for log sampling (0.0 to 1.0)

# Message Processing Limits
MAX_NARRATIVE_LENGTH=2000
# Maximum length for narrative extraction (100 to 10000)

MAX_MAXIM_LENGTH=500
# Maximum length for maxim extraction (10 to 1000)

# =============================================================================
# DOKKU-SPECIFIC CONFIGURATION
# =============================================================================

# These variables are typically set by Dokku automatically
# DOKKU_APP_NAME=chatops-agent
# DOKKU_APP_TYPE=web
# DOKKU_PROXY_PORT=80
# DOKKU_PROXY_SSL_PORT=443

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# For local development, you may want to override these values:
# ENVIRONMENT=development
# LOG_LEVEL=DEBUG
# LOG_SAMPLE_RATE=1.0
# UNION_ACTION_API_URL=http://localhost:8000

# =============================================================================
# PRODUCTION CONFIGURATION
# =============================================================================

# For production deployment, ensure these are set:
# ENVIRONMENT=production
# LOG_LEVEL=INFO
# LOG_SAMPLE_RATE=0.1
# UNION_ACTION_API_URL=http://localhost:8000

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - Never commit .env files to version control
# - Use Dokku config:set for production secrets
# - Rotate secrets regularly
# - Use environment-specific values

# =============================================================================
# DEPLOYMENT EXAMPLES
# =============================================================================

# Dokku deployment commands:
# dokku config:set chatops-agent ENVIRONMENT=production
# dokku config:set chatops-agent LOG_LEVEL=INFO
# dokku config:set chatops-agent UNION_ACTION_API_URL=http://localhost:8000
# dokku config:set chatops-agent LOG_SAMPLE_RATE=0.1
# dokku config:set chatops-agent MAX_NARRATIVE_LENGTH=2000
# dokku config:set chatops-agent MAX_MAXIM_LENGTH=500
